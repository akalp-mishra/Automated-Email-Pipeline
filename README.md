# Automated-Email-Pipeline
This is an automated email pipeline that automatically searches for the company, its details, adresses, generates a custom email, generates a ciutom brochure , supervises everything and send it automatically using api keys and locally run models for supervization
